# ===========================================
# Frontend (Vite - must prefix with VITE_)
# ===========================================
VITE_AIRTABLE_TOKEN=
VITE_CLERK_PUBLISHABLE_KEY=

# ===========================================
# Netlify Functions (set in Netlify dashboard for production)
# ===========================================
CLAUDE_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
PERPLEXITY_API_KEY=
AIRTABLE_API_KEY=
SENDGRID_API_KEY=
BRAND_DEV_API_KEY=

# Stripe (if using checkout) — use SECRET key (sk_...), NOT publishable (pk_...)
# Get keys: https://dashboard.stripe.com/account/apikeys
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Optional: 100% off coupon ID for testing (create in Stripe → Products → Coupons)
STRIPE_TEST_COUPON_ID=

# Clerk (if using auth)
CLERK_SECRET_KEY=
